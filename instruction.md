YOLO26 のデプス推定を使用した Unity プラグインを実装します。

モデルは yolo26n-depth の CoreML 版を使用します。この推論を動かすネイティブプラグインを作成して Unity 上で使えるようにする、ということです。

ネイティブプラグイン側ではなるべく推論だけを行うようにして、ポスト処理は Unity 上でシェーダーを使用して動かすようにします。

テストとして、WebCamTexture から取得した画像をリアルタイムにデプス推定するデモを作成します。ウェブカメラの入力は YOLO モデルへの入力形状に合わせてクロッピングしたものを使用します。

取得できたデプスは、決められたレンジで視覚化を行います。このレンジはスライダーで変更できます。

Unity プロジェクトは keijiro Unity Template Project を使用し、URP, UI-Toolkit を利用します。

なお、YOLO26 の CoreML の動作については ../yolo26-coreml-test で検証済みですので、参考になる部分は参考にしてください。
